@model UserEditViewModel

<div class="form-group">
    <label class="font-noraml">Role</label>

    <form asp-controller="Users" asp-action="UserEdit" class="m-t" id="roleChangeForm"
          data-ajax="true" data-ajax-method="POST" data-ajax-mode="REPLACE-WITH" data-ajax-update="#roleChangeForm" data-ajax-success="onRoleChangeSuccess">
        <input type="hidden" asp-for="UserId" />

        <div class="form-group">
            <select asp-for="Role" asp-items="ViewBag.Roles" class="chosen-select">
                <option value="">Alege un rol</option>
            </select>
        </div>

        <div class="form-group">
            <select asp-for="IdUserGroup" asp-items="ViewBag.UserGroups" class="chosen-select" data-placeholder="Alege un grup">
            </select>
        </div>
    </form>
</div>
<script>
    function onRoleChangeSuccess() {
        location.reload();
    }
</script>